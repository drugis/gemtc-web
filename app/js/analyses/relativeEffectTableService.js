'use strict';
define(['lodash'], function(_) {
  var dependencies = [];

  var RelativeEffectTableService = function() {
    return {
    };

  };

  return dependencies.concat(RelativeEffectTableService);
});
